// Exercise 14: Transforming members with `map` to be all written uppercase
// Write your code here:

const teamMembers = ["John", "Mike", "Alex", "Bobb"];

const teamMembersUppercase = (teamMembers.map(member => member.toUpperCase()));

console.log(teamMembersUppercase);
